<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>
  <title>Fitness Equipment App</title>
  <style>
    .card {
      width: 100%;
      display: flex;
      flex-direction: column;
      height: 200%;
    }
    .card-img-top {
      width: 100%;
      height: 300px; 
    }
    .card-body {
      flex: 1;
      display: flex;
      flex-direction: column;
      justify-content: space-between;
    }
    .card-title {
      font-size: 1.25rem;
    }
    .card-text {
      font-size: 1rem;
    }
  </style>
</head>
<body>
  <%- include('partials/navbar') %>

  <div class="container">
    <br>
    <div class="text-center"><h2>Items in the Fitness Equipment Website</h2></div>
    <br>

    <div class="row row-cols-1 row-cols-md-3 g-4">
      <% for(let i = 0; i < products.length; i++) { %>
        <div class="col">
          <div class="card h-100">
            <img class="card-img-top" src="images/<%= products[i].productImage %>" alt="Card image">
            <div class="card-body">
              <h4 class="card-title">
                <a href="/products/<%= products[i].productId %>"><%= products[i].productName %></a>
              </h4>
              <p class="card-text"><strong>Price:</strong> $<%= products[i].productPrice %></p>
              <p class="card-text"><strong>Quantity:</strong> <%= products[i].quantity %></p>
              <p class="card-text"><strong>Brand:</strong> <%= products[i].productBrand %></p>
              <div class="d-flex justify-content-between">
                <a href="/editProduct/<%= products[i].productId %>" class="btn btn-warning">Edit</a>
                <a href="/deleteProduct/<%= products[i].productId %>" class="btn btn-danger" onclick="return confirm('Are you sure you want to delete this product?')">Delete</a>
              </div>
            </div>
          </div>
        </div>
      <% } %>
    </div>
  </div>
</body>
</html>
